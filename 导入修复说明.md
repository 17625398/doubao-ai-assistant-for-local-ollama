# 设计系统导入修复说明

## 问题
Build Error: Module not found: Can't resolve '@doubao/core/design'

## 解决方案

### 1. 已创建的文件结构

```
packages/web/src/
├── design-system/          # 本地设计系统副本
│   ├── index.ts           # 模块入口
│   ├── tokens.ts          # 设计令牌
│   └── utils.ts           # 工具函数
├── app/
│   ├── design-system.css  # CSS 设计系统
│   └── page.tsx           # 主页面（已更新导入）
└── components/
    └── DesignSystemExample.tsx  # 示例组件（已更新导入）
```

### 2. 导入路径更新

**page.tsx** (第 54 行):
```tsx
import { getColor, getSpacing, getRadius, getShadow, createGradient } from '@/design-system'
```

**DesignSystemExample.tsx** (第 3 行):
```tsx
import { getColor, getSpacing, getRadius, getShadow, createGradient } from '@/design-system'
```

### 3. 配置更新

**next.config.js** - 添加了 Webpack 别名:
```javascript
config.resolve.alias = {
  ...config.resolve.alias,
  '@doubao/core': path.resolve(__dirname, '../core/src'),
  '@doubao/core/design': path.resolve(__dirname, '../core/src/design'),
};
```

**tsconfig.json** - 已有路径映射:
```json
{
  "paths": {
    "@/*": ["./src/*"],
    "@doubao/core": ["../core/src/index.ts"],
    "@doubao/core/*": ["../core/src/*"]
  }
}
```

### 4. Core 包更新

**packages/core/src/index.ts** - 添加设计系统导出:
```typescript
// 导出设计系统
export * from './design';
```

**packages/core/src/design/index.ts** - 新建模块入口:
```typescript
export * from './tokens';
export * from './utils';
```

## 验证步骤

### 1. 重启开发服务器
```bash
cd d:\Doubao\refactored\packages\web
npm run dev
```

### 2. 检查导入
如果仍然报错，尝试：
```bash
# 清除缓存
rm -rf .next
rm -rf node_modules/.cache

# 重新启动
npm run dev
```

### 3. 验证功能
访问 http://localhost:3000，检查：
- ✅ 页面正常加载
- ✅ 测试按钮显示渐变背景
- ✅ 悬浮效果正常
- ✅ 没有控制台错误

## 文件清单

### 新增文件
1. ✅ `packages/web/src/design-system/index.ts` (11 行)
2. ✅ `packages/web/src/design-system/tokens.ts` (260 行)
3. ✅ `packages/web/src/design-system/utils.ts` (260 行)
4. ✅ `packages/core/src/design/index.ts` (11 行)

### 修改文件
5. ✅ `packages/web/src/app/page.tsx` - 更新导入路径
6. ✅ `packages/web/src/components/DesignSystemExample.tsx` - 更新导入路径
7. ✅ `packages/web/next.config.js` - 添加 Webpack 别名
8. ✅ `packages/core/src/index.ts` - 添加设计系统导出

## 如果仍有问题

### 方案 A：使用相对路径
```tsx
// 在 page.tsx 中
import { getColor, getSpacing, getRadius, getShadow, createGradient } from '../design-system'
```

### 方案 B：直接使用工具文件
```tsx
// 在 page.tsx 中
import { getColor, getSpacing, getRadius, getShadow, createGradient } from '@/design-system/utils'
```

### 方案 C：内联定义（临时方案）
```tsx
// 临时直接在组件中定义颜色
const colors = {
  primary: { 500: '#6366f1' },
  secondary: { 500: '#d946ef' },
  success: { 500: '#22c55e' },
} as const
```

## 下一步

修复导入问题后，可以：
1. 重新构建 core 包（修复预先存在的 TS 错误）
2. 统一使用 `@doubao/core/design` 导入
3. 删除 web 包中的 design-system 副本
4. 使用 core 包的设计系统
